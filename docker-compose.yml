version: '3.8'
services:
  fuseki:
    image: stain/jena-fuseki:4.0.0
    container_name: jena-fuseki
    ports:
      - "3030:3030"
    environment:
      - ADMIN_PASSWORD=admin
    volumes:
      - fuseki-data:/fuseki

volumes:
  fuseki-data:
